# netlify.toml
# Tells Netlify how to build and serve this Vite React app.

[build]
  command   = "npm run build"
  publish   = "dist"

# Redirect all routes to index.html so React handles routing client-side.
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
